# Admin dashboard credentials (see README.md "Admin Dashboard" section for
# full details, including how these are used and their limitations).
#
# Copy this file to .env.local and change these before deploying anywhere
# public. .env.local is gitignored and is NOT committed to version control.
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=changeme123
